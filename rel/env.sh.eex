#!/bin/sh

# Fly.io uses IPv6 for internal networking and DNS.
# Configure the BEAM to resolve DNS over IPv6.
export RELEASE_DISTRIBUTION=sname
export RELEASE_NODE=lowendinsight_get
export ERL_AFLAGS="-proto_dist inet6_tcp -kernel inet_dist_use_interface {0,0,0,0,0,0,0,0}"
