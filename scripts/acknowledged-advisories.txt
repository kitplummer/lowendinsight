# Security advisories acknowledged after checking they cannot be fixed within
# our dependency constraints today. Read by scripts/dependency-audit.sh, which
# fails on any advisory not listed here and on any entry that no longer matches
# a locked dependency, so this file can only shrink by deleting lines.
#
# One advisory per line: its IDs, comma-separated, then " # " and the reason.
# Every entry needs a reason. Hex matches any of an advisory's IDs; mix_audit
# matches only GHSA IDs, so list the GHSA alias when there is one.

# decimal 2.4.1: fixed only in decimal 3.0, and ecto 3.13 requires ~> 2.0.
EEF-CVE-2026-32686,GHSA-rhv4-8758-jx7v # MEDIUM unbounded exponent; decimals come only from Postgres numeric columns and internal arithmetic, no request input is cast to :decimal

# cowlib 2.20.0 is the latest release; no fix published.
EEF-CVE-2026-43966,GHSA-w4f7-4cxr-rv3c # MEDIUM cow_http_struct_hd:escape_string/2, not called by cowboy, plug_cowboy, plug or this code
EEF-CVE-2026-43969,GHSA-g2wm-735q-3f56 # LOW cow_cookie:cookie/1 (request encoder), not called by cowboy, plug_cowboy, plug or this code
